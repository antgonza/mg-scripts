language: minimal
install:
  - tar zxvf data/FASTQ.tgz
  - mkdir trim-files
  - mkdir bin
  - echo 'echo "$@"' > bin/slurm
  - chmod +x bin/slurm
  - export PATH=${PATH}:${PWD}/bin
script:
  - sh bowtie_trim.sh ${PWD}/FASTQ $PWD/trim-files/ 3
  - return 0
